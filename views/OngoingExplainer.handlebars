
<header>
    <a href="/home/home/{{user.username}}"><img src="/images/logo.jpg" alt="" class="img-responsive" width="50px" height="50px"></a>
        <div class="nav-icon">
            <span></span>
            <span></span>
            <span></span>
        </div>
    </header>

<div class="wrapper">
                   <section class="titlebar">
            <h1 class="page-title"><span>Ongoing </span>Explainer Videos</h1>
            <div id="particles-js"></div>
        </section>

        
                        {{#each OngoingExplainer}}
     {{#if Ongoing}}
            <div class="cont">
            <section class="portfolio-single type-2 top_90">

                <div class="portfolio-images top_60 row">

                    {{#if Enable}}
                        <a href="/progress/OngoingExplainerLargeView/{{this._id}}/{{this.toWhom}}" class="col-md-12 news-list bottom_60">
                            <article class="news-box row">
                                <figure class="col-md-10">
                            <video src="{{this.explainer_final_path}}" class="js-player" controls data-plyr-config='{ "title": "This is an example video", "volume": 1, "debug": true }'></video>
                                </figure>
                                <div class="col-md-10 left-list">
                                    <small>{{this.videotype}}</small>
                                    <h4 class="title">{{this._id}}</h4>
                                    <p>Click Here to View Enlarge ...</p>
                                    <div class="information">{{this.owner}}, &nbsp; &nbsp;<span>{{this.explainer_date}}</span>&nbsp; &nbsp;<span>{{this.explainer_time}}</span></div>
                                </div>
                            </article>
                        </a>
                        {{/if}}

                </div>
            </section>
        </div> <!-- cont end -->
    
    
                    {{/if}}
                    {{/each}}
    
        
      
                    <div class="text-center">
                        
                                <a href="/progress/OngoingGiveMeAComment-Explainer/{{user._id}}/{{user.username}}" class="text-center">Click here Leave a Reply</a>
                    </div>

        

    
    
    </div>

